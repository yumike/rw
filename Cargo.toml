[workspace]
resolver = "3"
members = ["crates/*"]

[workspace.package]
version = "0.1.35"
edition = "2024"
rust-version = "1.97"
license = "MIT OR Apache-2.0"

[workspace.dependencies]
# Internal crates
rw-assets = { path = "crates/rw-assets" }
rw-cache = { path = "crates/rw-cache" }
rw-comments = { path = "crates/rw-comments" }
rw-cache-s3 = { path = "crates/rw-cache-s3" }
rw-config = { path = "crates/rw-config" }
rw-confluence = { path = "crates/rw-confluence" }
rw-diagrams = { path = "crates/rw-diagrams" }
rw-kroki = { path = "crates/rw-kroki" }
rw-embedded-preview = { path = "crates/rw-embedded-preview" }
rw-git-fixture = { path = "crates/rw-git-fixture" }
rw-meta = { path = "crates/rw-meta" }
rw-parser = { path = "crates/rw-parser" }
rw-plantuml = { path = "crates/rw-plantuml" }
rw-renderer = { path = "crates/rw-renderer" }
rw-sections = { path = "crates/rw-sections" }
rw-server = { path = "crates/rw-server" }
rw-server-info = { path = "crates/rw-server-info" }
rw-site = { path = "crates/rw-site" }
rw-storage = { path = "crates/rw-storage" }
rw-storage-fs = { path = "crates/rw-storage-fs" }
rw-storage-s3 = { path = "crates/rw-storage-s3" }
rw-vcs = { path = "crates/rw-vcs" }
# External dependencies
# The feature list is aws-sdk-s3's own `default` minus `rustls`. That feature is
# a legacy alias for aws-smithy-runtime/tls-rustls, which pulls a second TLS
# stack — rustls 0.21 and hyper-rustls 0.24 — in beside the rustls 0.23 stack
# `default-https-client` already provides and that the client actually uses.
aws-sdk-s3 = { version = "1", default-features = false, features = [
  "sigv4a",
  "http-1x",
  "default-https-client",
  "rt-tokio",
] }
base64 = "0.23"
glob = "0.3"
notify = "8"
parking_lot = "0.12"
quick-xml = "0.42"
thiserror = "2"
serde = { version = "1.0", features = ["derive"] }
toml = "1.0"
hex = "0.4"
ignore = "0.4"
pretty_assertions = "1.4"
pulldown-cmark = { version = "0.13", default-features = false, features = ["simd"] }
rayon = "1.10"
regex = "1.11"
serde_json = "1"
sqlx = { version = "0.9", default-features = false, features = ["runtime-tokio", "sqlite"] }
uuid = { version = "1", features = ["v4", "serde"] }
serde_yaml = "0.9"
sha2 = "0.11"
static_assertions = "1.1"
tempfile = "3"
tracing = "0.1"
ureq = { version = "3", features = ["rustls", "json"] }

shellexpand = "3"

# Benchmarking.
# CodSpeed's drop-in for Divan: aliased to `divan` so bench code stays vanilla.
# Runs as normal Divan locally (`cargo bench`); instruments under CodSpeed CI.
divan = { package = "codspeed-divan-compat", version = "5" }

[workspace.lints.clippy]
pedantic = { level = "warn", priority = -2 }
# Allowed pedantic lints
missing_errors_doc = "allow"
missing_panics_doc = "allow"
module_name_repetitions = "allow"
must_use_candidate = "allow"
# Restriction lints
disallowed_types = "warn"
clone_on_ref_ptr = "warn"
dbg_macro = "warn"
empty_drop = "warn"
empty_structs_with_brackets = "warn"
exit = "warn"
get_unwrap = "warn"
print_stdout = "warn"
print_stderr = "warn"
rc_buffer = "warn"
rc_mutex = "warn"
rest_pat_in_fully_bound_structs = "warn"
str_to_string = "warn"
todo = "warn"
unimplemented = "warn"
verbose_file_reads = "warn"
# Nursery lints
debug_assert_with_mut_call = "warn"
redundant_clone = "warn"
unused_peekable = "warn"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
